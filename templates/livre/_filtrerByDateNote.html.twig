{{ form_start(form) }}
<div class="row">
	<div class="col s6">
		{{ form_row(form.date_debut) }}
	</div>
    <div class="col s6">
		{{ form_row(form.date_fin) }}
	</div>
    <div class="col s6">
		{{ form_row(form.note_min) }}
	</div>
	<div class="col s6">
		{{ form_row(form.note_max) }}
	</div>
</div>
<button class="btn">{{ button_label|default('Filtrer') }}</button>
{{ form_end(form) }}
