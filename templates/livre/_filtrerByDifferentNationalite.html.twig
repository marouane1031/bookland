{{ form_start(form) }}
<div class="row">
	<div class="col s12">
		{{ form_row(form.nationalite) }}
	</div>
</div>
<button class="btn">{{ button_label|default('Filtrer') }}</button>
{{ form_end(form) }}
